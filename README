This package is still under development.  The main library seems pretty
stable API-wise, so it's not likely to change much from here on.  The
individual modules aren't quite where I'd like them to be, but they're
getting there.
